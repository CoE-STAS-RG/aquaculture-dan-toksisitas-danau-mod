services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: AquaCulture
      MYSQL_USER: admin
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: root
    ports:
      # host:container
      - '3030:3306'
    volumes:
      - aquaculture:/var/lib/mysql


volumes:
  aquaculture: